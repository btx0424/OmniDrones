name: Forest

defaults:
  - /env_base
  - /sim_base

env:
  num_envs: 4
  env_spacing: 40
  max_episode_length: 800
  
drone_model: Firefly

forest_length: 20
forest_width: 12
forest_spacing: 1.8

multidiscrete_action:
  nbins: 4

# discrete_action:
#   nbins: 2