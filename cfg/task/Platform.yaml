name: Platform

defaults:
  - /env_base
  - /sim_base

env:
  num_envs: 1024

drone_model: Hummingbird

reward_effort_weight: 0.1
reward_distance_scale: 1.0

multidiscrete_action:
  nbins: 4

# discrete_action:
#   nbins: 2