name: PlatformHover

defaults:
  - /env_base
  - /sim_base

env:
  num_envs: 2048

drone_model: Hummingbird
num_drones: 4

reward_effort_weight: 0.1
reward_distance_scale: 1.0
arm_length: 0.85
joint_damping: 0.001

flatten_obs: true
flatten_state: true

action_transform: null
