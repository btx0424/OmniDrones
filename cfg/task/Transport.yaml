name: Transport

defaults:
  - /env_base
  - /sim_base

env:
  num_envs: 1024

drone_model: Hummingbird

payload_mass_min: 1.5
payload_mass_max: 2.5

reward_effort_weight: 0.1
reward_distance_scale: 0.5

# multidiscrete_action:
#   nbins: 4

discrete_action:
  nbins: 2
